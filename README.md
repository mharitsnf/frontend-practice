# Frontend Practice

I have no live server at the moment, but feel free to clone this repository and run it locally. I will post an update if a live server is finally available.

## Running locally

After cloning the repository, go to the directory, and then run:

```
npm i
npm run dev
```